# Class task | Questions

----

1. Explore different dictionary problems

2. Explore different condition and branching problems

3. Create a program where:
A secret number is stored inside the program.
User keeps entering numbers.
If the number matches, print:
`Correct Guess!`
Use break to stop the loop.
If wrong, print:
`Try Again`

4. Create a simple ATM menu.
```
Menu:
1. Check Balance
2. Deposit
3. Exit
```


5. Write a function area(length, breadth) that returns the
area of rectangle.

6. Write a Python program using User Defined Function
(UDF) that:
Takes two numbers from the user
Takes an operator sign `(+, -, *, /)`
Performs the selected operation
Displays the result


7. Write a user-defined function that takes 5 user inputs.
If the user enters an integer, add it to a list 
If the user enters a float number, remove the last element 
Finally, display the updated list after each operation